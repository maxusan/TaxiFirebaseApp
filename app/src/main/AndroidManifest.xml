<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bananadolphin.taxifirebaseapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaxiFirebaseApp">
        <activity android:name=".DriverSignInActivity"
            android:label="Driver Sign In"/>
        <activity android:name=".PassеngerSignInActivity"
            android:label="Passenger Sign In"/>
        <activity android:name=".ChangeModeActivity"
            android:label="@string/choose_login_mode_label"/>
        <activity android:name=".SplashScreenActivity"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>